tags = ['python', 'development', 'tutorials', 'code']

# this is a good method for trouble shooting imbalanced lists pass throughs
number_of_tags = len(tags)
last_item = tags[-1]
index_of_last_item = tags.index(last_item)

print(number_of_tags)
print(last_item)
print(index_of_last_item)
# end >>>  imbalanced lists pass throughs troubleshooting

# instructors notes:
# tags = ['python', 'development', 'tutorials', 'code']

# number_of_tags = len(tags) # length function gives you the count of elements in a list-pass in tags(list) as an argument
# last_item = tags[-1] # to grab the last items index
# index_of_last_item = tags.index(last_item) # call the list then call the last_item and pass it to the index

# print(number_of_tags)
# print(last_item)
# print(index_of_last_item)
